import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';

class RepayEmiScreen extends ConsumerWidget {
  const RepayEmiScreen({super.key});

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    return Scaffold(
      appBar: AppBar(title: const Text('RepayEmiScreen')),
      body: Center(
        child: Text('RepayEmiScreen - Implementation in progress'),
      ),
    );
  }
}
